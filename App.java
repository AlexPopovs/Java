public class App {

    // psvm + tab -> create main start method
    public static void main(String[] args) {


        // make cleaner text : cmd + option + L

     /*   boolean cat1 = true;
        boolean cat2 = false;
        String mouse = "Miki";  // default value of string is null (null value)
        int dogSize = 5;
        dogSize = 10; // OVERRIDING VALUE FROM 5 TO 10
        char myLetter = '%';         // char - Character - only one symbol is possible to write


        System.out.println(String.valueOf(myLetter));  // print out: sout ; print out ONLY for Strings, we translating e.g. int to string!!!
        // printing not names but values; that's why
      */
    }
}
